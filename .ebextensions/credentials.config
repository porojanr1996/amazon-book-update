files:
  "/var/app/current/credentials.json":
    mode: "000400"
    owner: webapp
    group: webapp
    content: |
      # Acest fișier va fi înlocuit cu credentials.json real
      # Folosește AWS Secrets Manager pentru producție
      # Sau upload manual prin: eb setenv sau AWS Console

